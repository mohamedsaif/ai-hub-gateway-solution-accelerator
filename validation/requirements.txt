# This is a comprehensive requirements.txt file for all labs to ensure that one installation covers everything.

# Do not include azure-functions-worker in this file
# The Python Worker is managed by the Azure Functions platform
# Manually managing azure-functions-worker may cause unexpected issues

# Azure CLI is a system prerequisite, not a Python dependency. Install it using
# https://learn.microsoft.com/cli/azure/install-azure-cli and verify with `az --version`.
# The notebooks invoke the external `az` command and do not import `azure.cli`.

requests
jsonref
python-dotenv
numpy
pandas
matplotlib
redis
msal
openai
openai[realtime]
azure-identity
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation-openai-v2
opentelemetry-semantic-conventions-ai
azure-monitor-opentelemetry
azure-monitor-ingestion
azure-ai-projects
azure-ai-agents
azure-ai-inference[prompts]
azure-search-documents
azure-functions
azure-mgmt-apimanagement

# MCP Labs
mcp
autogen-core 
autogen-ext[openai,azure,mcp] 
autogen-agentchat
semantic-kernel[mcp]

# Agent Framework
agent-framework
agent-framework-core
langchain
langchain-openai